# pastor_tracker

Cinematic auto-tracker for a single primary speaker — Python 3.12 + Arduino stepper pan.

Phase 1 scaffold. See `.planning/PROJECT.md` for full context.

## Quick start

```powershell
cd pastor_tracker
uv sync
uv run pytest tests
uv run ruff check src
uv run mypy src
```
