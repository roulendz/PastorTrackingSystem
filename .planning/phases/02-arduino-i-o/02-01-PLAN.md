---
phase: 02-arduino-i-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pastor_tracker/pyproject.toml
  - pastor_tracker/src/pastor_tracker/io/arduino_protocol.py
  - pastor_tracker/tests/test_arduino_protocol.py
autonomous: true
requirements:
  - IO-ARD-04
tags:
  - arduino
  - protocol
  - parser
  - pure-core
must_haves:
  truths:
    - "parse_line(b'READY:v2') returns Ready(version=2) — exact equality"
    - "parse_line(b'FB:1.50,2.00,1234.5,1,123456,42,2') returns Feedback DTO with all 7 fields"
    - "parse_line raises ProtocolParseError on b'GIBBERISH', b'', b'FB:1.5,2.0', b'ERROR: missing code'"
    - "parse_line(b'ERROR:99 - future code') accepts forward-compat unknown error codes"
    - "All 12 ErrorCode members (NONE..HEARTBEAT_TIMEOUT) exist with values 0..11"
    - "All four SETTINGS forms parse: textual to SettingsInfo, structured 5-tuple to Settings"
    - "100% branch coverage on src/pastor_tracker/io/arduino_protocol.py"
    - "ruff + mypy --strict clean on arduino_protocol.py"
    - "arduino_protocol.py imports no serial/threading/asyncio/os/time"
  artifacts:
    - path: "pastor_tracker/pyproject.toml"
      provides: "pyserial>=3.5,<4.0 + pytest-cov>=5.0,<7.0 deps"
      contains: "pyserial>="
    - path: "pastor_tracker/src/pastor_tracker/io/arduino_protocol.py"
      provides: "ProtocolEvent union DTOs (11 classes), ErrorCode IntEnum, parse_line() pure parser, ProtocolParseError, protocol constants mirrored from protocol.h"
      min_lines: 180
    - path: "pastor_tracker/tests/test_arduino_protocol.py"
      provides: "Parametrized parser tests covering 9 prefixes + 4 SETTINGS forms + malformed paths + forward-compat ERROR codes"
      min_lines: 80
  key_links:
    - from: "pastor_tracker/src/pastor_tracker/io/arduino_protocol.py"
      to: "arduino/stepper_controller/include/protocol.h"
      via: "constant mirror with line-number citations"
      pattern: "protocol\\.h:"
    - from: "pastor_tracker/tests/test_arduino_protocol.py"
      to: "pastor_tracker/src/pastor_tracker/io/arduino_protocol.py"
      via: "from pastor_tracker.io.arduino_protocol import parse_line, Ready, ..."
      pattern: "from pastor_tracker\\.io\\.arduino_protocol import"
---

<objective>
Land the **pure** half of Arduino I/O: protocol DTOs, ErrorCode IntEnum, protocol-constant mirror, parse_line() pure parser, ProtocolParseError, plus parametrized tests achieving 100% branch coverage. Also add pyserial (production runtime) and pytest-cov (coverage gate tooling) to pyproject.toml.

Purpose: this plan is the seed for Plans 02-02 (transport) and 02-03 (orchestrator). The parser stays side-effect-free (no serial/threading/asyncio imports) so its 100% branch coverage is achievable with zero fixtures.

Output:
- Updated pastor_tracker/pyproject.toml with two new dep entries
- pastor_tracker/src/pastor_tracker/io/arduino_protocol.py (~200 LOC, PURE)
- pastor_tracker/tests/test_arduino_protocol.py (~150 LOC parametrized tests)
- All Phase 1 quality gates green: ruff, mypy --strict, pytest, coverage
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-arduino-i-o/02-CONTEXT.md
@.planning/phases/02-arduino-i-o/02-RESEARCH.md
@.planning/phases/02-arduino-i-o/02-PATTERNS.md
@.planning/phases/02-arduino-i-o/02-VALIDATION.md
@arduino/stepper_controller/include/protocol.h
@pastor_tracker/src/pastor_tracker/core/types.py
@pastor_tracker/src/pastor_tracker/core/geometry.py
@pastor_tracker/pyproject.toml
@pastor_tracker/tests/conftest.py

<interfaces>
This plan EXPORTS from `pastor_tracker.io.arduino_protocol` (Plans 02-02 / 02-03 import these by exact name):

```python
PROTOCOL_VERSION_MAJOR: Final[int] = 2                  # protocol.h:12
FIRMWARE_INPUT_BUFFER_SIZE: Final[int] = 48             # protocol.h:39
FIRMWARE_INPUT_BUFFER_USABLE: Final[int] = 47
FIRMWARE_HEARTBEAT_TIMEOUT_MS: Final[int] = 1_000       # protocol.h:33
FIRMWARE_FEEDBACK_INTERVAL_MS: Final[int] = 20          # protocol.h:36
SEQ_MODULUS: Final[int] = 1 << 32
FEEDBACK_SEQ_GAP_WARN_THRESHOLD: Final[int] = 5
MAX_RX_LINE_BYTES: Final[int] = 256                     # T-02-04 DoS guard

class ErrorCode(IntEnum):  # 12 members, values 0..11 — mirrors protocol.h:67-80

# Frozen Pydantic v2 DTOs — every event inherits from private _Event(BaseModel):
class Ready(_Event):           version: int = Field(ge=1, le=255)
class Feedback(_Event):        7 fields (see <behavior> Task 2)
class Settings(_Event):        max_speed/max_accel/pid_p/pid_i/pid_d: float
class SettingsInfo(_Event):    message: str
class Limits(_Event):          min_deg/max_deg: float, validator max>min
class Driver(_Event):          enabled: bool
class Reset(_Event):           pass        # ack of host R command
class Stop(_Event):            pass        # ack of host E command
class Diag(_Event):            steps: int
class Error(_Event):           code: ErrorCode | int; message: str
class FeedbackHeader(_Event):  pass        # boot/recovery banner

ProtocolEvent: TypeAlias = Ready | Feedback | Settings | SettingsInfo | Limits | Driver | Reset | Stop | Diag | Error | FeedbackHeader

class ProtocolParseError(Exception): ...

def parse_line(line: bytes) -> ProtocolEvent: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pyserial + pytest-cov to pyproject.toml and uv sync</name>
  <files>pastor_tracker/pyproject.toml</files>
  <read_first>
    - pastor_tracker/pyproject.toml lines 7-12 (existing [project.dependencies] array)
    - pastor_tracker/pyproject.toml lines 14-23 (existing [dependency-groups].dev array)
    - pastor_tracker/pyproject.toml lines 79-81 (mypy overrides — DO NOT modify; serial.* already listed)
    - 02-PATTERNS.md lines 819-841 (exact diff sketch + mypy-overrides rationale)
    - 02-RESEARCH.md lines 1043-1056 (Environment Availability — version pins justified)
    - 02-VALIDATION.md lines 65-67 (Wave 0 dep-add checklist items)
  </read_first>
  <action>
    Edit pastor_tracker/pyproject.toml:

    1. In [project.dependencies] (lines 7-12), append after `"numpy>=2.4,<3.0",`:
       `    "pyserial>=3.5,<4.0",`
       Final array order: pydantic, pydantic-settings, structlog, numpy, pyserial.

    2. In [dependency-groups].dev (lines 14-23), append after `"pre-commit>=4.0,<5.0",`:
       `    "pytest-cov>=5.0,<7.0",`

    3. DO NOT modify [[tool.mypy.overrides]] lines 79-81. The `serial.*` entry is already present and is correct (pyserial does not ship complete type stubs).

    4. From the pastor_tracker/ directory: `uv sync` — regenerates uv.lock with the two new deps.

    5. Verify deps importable: `pastor_tracker\.venv\Scripts\python.exe -c "import serial; import pytest_cov; print(serial.__version__)"` — expect a 3.5.x string, no ImportError.

    Tiger-style: do not widen bounds (4.x of pyserial / 7.x of pytest-cov are unvetted).
  </action>
  <verify>
    <automated>cd pastor_tracker; uv sync; .venv\Scripts\python.exe -c "import serial, pytest_cov; print('ok')"; .venv\Scripts\python.exe -m pytest -ra</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "pyserial>=3.5,<4.0" pastor_tracker/pyproject.toml` finds exactly 1 line.
    - `grep -F "pytest-cov>=5.0,<7.0" pastor_tracker/pyproject.toml` finds exactly 1 line.
    - `pastor_tracker\.venv\Scripts\python.exe -c "import serial; import pytest_cov"` exits 0.
    - `pastor_tracker/uv.lock` contains string `pyserial` (grep finds ≥ 1 hit).
    - `pastor_tracker\.venv\Scripts\python.exe -m pytest -ra` exits 0 (Phase 1 baseline preserved, no regression).
    - `git diff pastor_tracker/pyproject.toml` shows zero changes inside the [[tool.mypy.overrides]] block (serial.* untouched).
  </acceptance_criteria>
  <done>Both deps locked, importable, Phase 1 tests still green. Conventional Commit: `chore(02): add pyserial 3.5 + pytest-cov 5 deps`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create arduino_protocol.py — DTOs, ErrorCode enum, constants, parse_line()</name>
  <files>pastor_tracker/src/pastor_tracker/io/arduino_protocol.py</files>
  <read_first>
    - pastor_tracker/src/pastor_tracker/core/types.py lines 17-24 (imports), 33-41 (named constants), 87-90 (`_FrozenModel` base — TEMPLATE for `_Event`), 100-122 (`Detection` field+validator pattern)
    - pastor_tracker/src/pastor_tracker/core/geometry.py lines 32-54 (pure-function + guard-clause pattern for parse_line)
    - pastor_tracker/src/pastor_tracker/config.py lines 31-43 (named-constant + firmware-citation comment style)
    - 02-PATTERNS.md lines 38-138 (full pattern map for arduino_protocol.py)
    - 02-PATTERNS.md lines 845-866 (`_Event` base + tiger-style guard format)
    - 02-RESEARCH.md lines 105-188 (Protocol Reference — wire format, RX line catalog, 4 SETTINGS forms, error-code 0-11, parser branch matrix)
    - 02-RESEARCH.md lines 943-1024 (Code Examples — parse_line ladder + ErrorCode enum + constants)
    - 02-RESEARCH.md lines 723-755 (ProtocolParseError lives here)
    - 02-RESEARCH.md lines 859-867 (Pitfall 9 — accept ERROR:0 + forward-compat for unknown codes)
    - arduino/stepper_controller/include/protocol.h lines 12, 33, 36, 39, 67-80 (constants + ErrorCode source of truth)
  </read_first>
  <behavior>
    - parse_line(b"READY:v2") == Ready(version=2)
    - parse_line(b"READY:v3") == Ready(version=3)  (forward-compat: parser does NOT enforce version match — handshake's job)
    - parse_line(b"FB:1.50,2.00,1234.5,1,123456,42,2") == Feedback(current_angle_deg=1.5, target_angle_deg=2.0, speed_steps_per_sec=1234.5, is_running=True, timestamp_micros=123456, sequence=42, accel_phase=2)
    - parse_line(b"FB:-90.00,-90.00,0.00,0,0,0,0") == Feedback(... is_running=False, accel_phase=0)
    - parse_line(b"FB_HEADER:currentAngle,targetAngle,...") == FeedbackHeader()
    - parse_line(b"SETTINGS: defaults (no valid EEPROM)") == SettingsInfo(message="defaults (no valid EEPROM)")
    - parse_line(b"SETTINGS: loaded from EEPROM") == SettingsInfo(message="loaded from EEPROM")
    - parse_line(b"SETTINGS: saved to EEPROM") == SettingsInfo(message="saved to EEPROM")
    - parse_line(b"SETTINGS:25000.00,12500.00,1.00,0.00,0.10") == Settings(max_speed=25000.0, max_accel=12500.0, pid_p=1.0, pid_i=0.0, pid_d=0.1)  (5-comma structured form)
    - parse_line(b"LIMITS:-90.00,90.00") == Limits(min_deg=-90.0, max_deg=90.0)
    - Limits(min_deg=90.0, max_deg=-90.0) raises ValidationError (max<=min invariant)
    - parse_line(b"DRIVER:ENABLED") == Driver(enabled=True), parse_line(b"DRIVER:DISABLED") == Driver(enabled=False)
    - parse_line(b"RESET:OK") == Reset(); parse_line(b"STOP:OK") == Stop()
    - parse_line(b"DIAG: moving 100 steps") == Diag(steps=100); parse_line(b"DIAG: moving -200 steps") == Diag(steps=-200)
    - parse_line(b"ERROR:11 - PC heartbeat lost") == Error(code=ErrorCode.HEARTBEAT_TIMEOUT, message="PC heartbeat lost")
    - parse_line(b"ERROR:0 - sentinel") == Error(code=ErrorCode.NONE, message="sentinel")  (Pitfall 9 — accept code 0)
    - parse_line(b"ERROR:99 - future code") returns Error with code=99 (kept as int) — forward-compat
    - parse_line raises ProtocolParseError on: b"", b"GIBBERISH", b"FB:1.5,2.0" (too few fields), b"FB:nan,2.0,3.0,1,4,5,0" (NaN), b"FB:1.0,2.0,inf,1,4,5,0" (infinity), b"ERROR: missing code", b"ERROR:11" (no separator), b"DIAG: moving abc steps", b"DRIVER:HALFON", b"SETTINGS:1.0,2.0,3.0" (3 fields not 5)
    - parse_line(b"X" * 257) raises ProtocolParseError (T-02-04 DoS guard)
    - All DTO classes are frozen (assigning a field raises ValidationError) and reject extras (Ready(version=2, foo=1) raises)
    - Module imports with NO serial/threading/asyncio/os/time imports (purity gate)
  </behavior>
  <action>
    Create pastor_tracker/src/pastor_tracker/io/arduino_protocol.py with the following structure:

    1. Module docstring (mirror core/types.py:1-16 shape — paragraph + mutation pattern + invariant note + firmware citations to main.cpp lines 417-422 boot preamble, 149-162 FB emission, 63-66 ERROR emission, 235-244 settings echo, 273-276 limits echo).

    2. Imports (Phase 1 ordering — `__future__`, stdlib, third-party):
       ```
       from __future__ import annotations
       import math
       import re
       from enum import IntEnum
       from typing import Final, Literal, TypeAlias

       from pydantic import BaseModel, ConfigDict, Field, ValidationError, model_validator
       ```

    3. Module constants (verbatim names from 02-RESEARCH.md Code Examples, with `# protocol.h:NN` citations):
       PROTOCOL_VERSION_MAJOR=2 (protocol.h:12), FIRMWARE_INPUT_BUFFER_SIZE=48 (protocol.h:39), FIRMWARE_INPUT_BUFFER_USABLE=47, FIRMWARE_HEARTBEAT_TIMEOUT_MS=1_000 (protocol.h:33), FIRMWARE_FEEDBACK_INTERVAL_MS=20 (protocol.h:36), SEQ_MODULUS=1<<32, FEEDBACK_SEQ_GAP_WARN_THRESHOLD=5 (IO-ARD-04), MAX_RX_LINE_BYTES=256 (T-02-04 DoS guard). Each `Final[int]`.

    4. ErrorCode IntEnum — 12 members verbatim from protocol.h:67-80: NONE=0, EMPTY_COMMAND=1, UNKNOWN_COMMAND_TYPE=2, MOVE_MISSING_ARGUMENT=3, DIAGNOSTIC_MISSING_ARGUMENT=4, SETTINGS_MISSING_ARGUMENT=5, DRIVER_MISSING_ARGUMENT=6, DRIVER_INVALID_ARGUMENT=7, HOMING_FAILED=8, ANGLE_OUT_OF_BOUNDS=9, SETTINGS_OUT_OF_BOUNDS=10, HEARTBEAT_TIMEOUT=11.

    5. `_Event(BaseModel)` private base — verbatim mirror of `core/types.py:_FrozenModel`:
       ```
       class _Event(BaseModel):
           """Base for all parsed protocol events — frozen + extra-forbidden."""
           model_config = ConfigDict(frozen=True, extra="forbid")
       ```

    6. 11 DTO classes inheriting `_Event` per <interfaces> block. Specifically:
       - `Ready`: `version: int = Field(ge=1, le=255)`
       - `Feedback`: `current_angle_deg: float`, `target_angle_deg: float`, `speed_steps_per_sec: float`, `is_running: bool`, `timestamp_micros: int = Field(ge=0)`, `sequence: int = Field(ge=0)`, `accel_phase: Literal[0, 1, 2, 3]`
       - `Settings`: 5 floats `max_speed`, `max_accel`, `pid_p`, `pid_i`, `pid_d`
       - `SettingsInfo`: `message: str = Field(min_length=1)`
       - `Limits`: `min_deg: float`, `max_deg: float`, plus `@model_validator(mode="after")` enforcing `max_deg > min_deg` with informative ValueError (mirror of Detection bbox validator at types.py:110-122)
       - `Driver`: `enabled: bool`
       - `Reset`, `Stop`, `FeedbackHeader`: empty (just `pass` after docstring)
       - `Diag`: `steps: int`
       - `Error`: `code: ErrorCode | int` (union enables forward-compat — known codes coerced to enum, unknown ints kept). `message: str = Field(min_length=1)`. NB: `ErrorCode | int` is fine under mypy `disallow_any_explicit` (both concrete types).

    7. ProtocolEvent TypeAlias = union of all 11 DTOs.

    8. `class ProtocolParseError(Exception)` — docstring "RX line did not match any known prefix or had bad fields. - IO-ARD-04".

    9. `parse_line(line: bytes) -> ProtocolEvent` — flat prefix-dispatch ladder per 02-RESEARCH.md lines 990-1005 verbatim:
       - Guard 1: empty line → ProtocolParseError("empty line")
       - Guard 2: len(line) > MAX_RX_LINE_BYTES → ProtocolParseError(f"line length {len(line)} exceeds MAX_RX_LINE_BYTES={MAX_RX_LINE_BYTES}")
       - Strip `b"\r\n"` then ascii decode (UnicodeDecodeError → ProtocolParseError)
       - Prefix dispatch ladder: FB: → _parse_fb, FB_HEADER: → FeedbackHeader(), READY:v → _parse_ready, ERROR: → _parse_error, SETTINGS: → _parse_settings, LIMITS: → _parse_limits, DRIVER: → _parse_driver, RESET:OK → Reset(), STOP:OK → Stop(), DIAG: → _parse_diag
       - Fall-through → ProtocolParseError(f"unknown prefix: {text[:16]!r}")

    10. Private `_parse_*` helpers — each:
        - Parses field tokens, enforces field count.
        - Calls `float()` then `math.isfinite()` on every float; raises ProtocolParseError on NaN/inf (T-02-04 Float-parse DoS guard, RESEARCH Security Domain row 2).
        - Wraps `ValueError`/`ValidationError` in ProtocolParseError using `raise ... from exc` (ruff B904).
        - ≤ 2-level nesting (CLAUDE.md rule 5) — guard clauses → happy path → return.

    11. `_parse_settings` discriminates 4 forms — detection rule: structured form has NO space after `SETTINGS:` and contains exactly 4 commas (5 fields); textual forms have leading space `SETTINGS: <text>`. The 3 textual forms (`defaults (no valid EEPROM)`, `loaded from EEPROM`, `saved to EEPROM`) all become `SettingsInfo(message=<text-after-space>)`. Anything else → ProtocolParseError.

    12. `_parse_error` — code parsed as int. If int in `ErrorCode._value2member_map_`, coerce to `ErrorCode(int)`; else keep raw int (Pitfall 9). Message is whatever follows ` - ` separator. Missing ` - ` → ProtocolParseError.

    13. `_parse_diag` — regex `r"^DIAG: moving (-?\d+) steps$"`. Match group → int. No match → ProtocolParseError.

    Tiger-style invariants throughout:
    - No `Any`. No `print`. No bare except. No `except Exception: pass`. No magic numbers (every literal in `Final` const). No serial/threading/asyncio/os/time imports.
    - mypy --strict + disallow_any_explicit clean. ruff (incl. ANN, T20, BLE, S, PLR2004, B904) clean.
  </action>
  <verify>
    <automated>cd pastor_tracker; .venv\Scripts\ruff.exe check src/pastor_tracker/io/arduino_protocol.py; .venv\Scripts\mypy.exe --strict src/pastor_tracker/io/arduino_protocol.py; .venv\Scripts\python.exe -c "from pastor_tracker.io.arduino_protocol import parse_line, Ready, Feedback, Settings, SettingsInfo, Limits, Driver, Reset, Stop, Diag, Error, FeedbackHeader, ErrorCode, ProtocolEvent, ProtocolParseError, PROTOCOL_VERSION_MAJOR, FIRMWARE_INPUT_BUFFER_USABLE, FEEDBACK_SEQ_GAP_WARN_THRESHOLD, SEQ_MODULUS, MAX_RX_LINE_BYTES; print('imports ok')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at pastor_tracker/src/pastor_tracker/io/arduino_protocol.py with ≥ 180 LOC.
    - Purity gate: `grep -E '^(import|from) (serial|threading|asyncio|os|time)' pastor_tracker/src/pastor_tracker/io/arduino_protocol.py` returns 0 lines.
    - `grep -c "class ErrorCode(IntEnum):" pastor_tracker/src/pastor_tracker/io/arduino_protocol.py` == 1.
    - All 12 ErrorCode members present: `grep -E "^    (NONE|EMPTY_COMMAND|UNKNOWN_COMMAND_TYPE|MOVE_MISSING_ARGUMENT|DIAGNOSTIC_MISSING_ARGUMENT|SETTINGS_MISSING_ARGUMENT|DRIVER_MISSING_ARGUMENT|DRIVER_INVALID_ARGUMENT|HOMING_FAILED|ANGLE_OUT_OF_BOUNDS|SETTINGS_OUT_OF_BOUNDS|HEARTBEAT_TIMEOUT) =" pastor_tracker/src/pastor_tracker/io/arduino_protocol.py | wc -l` == 12.
    - All 11 DTO classes: `grep -cE "^class (Ready|Feedback|Settings|SettingsInfo|Limits|Driver|Reset|Stop|Diag|Error|FeedbackHeader)\(_Event\):" pastor_tracker/src/pastor_tracker/io/arduino_protocol.py` == 11.
    - `grep -c "^ProtocolEvent: TypeAlias" pastor_tracker/src/pastor_tracker/io/arduino_protocol.py` == 1.
    - `grep -c "^def parse_line(line: bytes) -> ProtocolEvent:" pastor_tracker/src/pastor_tracker/io/arduino_protocol.py` == 1.
    - `pastor_tracker\.venv\Scripts\ruff.exe check pastor_tracker/src/pastor_tracker/io/arduino_protocol.py` exits 0.
    - `pastor_tracker\.venv\Scripts\mypy.exe --strict pastor_tracker/src/pastor_tracker/io/arduino_protocol.py` exits 0.
    - Pure-import smoke: `pastor_tracker\.venv\Scripts\python.exe -c "from pastor_tracker.io.arduino_protocol import parse_line"` exits 0.
    - `grep -c "protocol\\.h:" pastor_tracker/src/pastor_tracker/io/arduino_protocol.py` ≥ 5 (firmware citations).
  </acceptance_criteria>
  <done>arduino_protocol.py exists, lints + types clean, imports cleanly with no I/O. Conventional Commit: `feat(02): pure arduino protocol parser + DTOs (IO-ARD-04 part 1)`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Parametrized parser tests achieving 100% branch coverage</name>
  <files>pastor_tracker/tests/test_arduino_protocol.py</files>
  <read_first>
    - pastor_tracker/tests/test_geometry.py lines 1-24 (test docstring + named constants + parametrize style)
    - pastor_tracker/tests/test_types.py lines 95-122 (parametrized rejection + `pytest.raises(ValidationError, match="…")` shape)
    - pastor_tracker/tests/conftest.py (no fixture needed — parser is pure)
    - pastor_tracker/pyproject.toml lines 61-62 (tests can use literals: ANN/PLR2004/S ignored)
    - 02-PATTERNS.md lines 415-507 (full pattern map for test_arduino_protocol.py)
    - 02-RESEARCH.md lines 619-654 (parametrized table + branch matrix)
    - 02-RESEARCH.md lines 178-187 (parser branch matrix)
    - 02-VALIDATION.md row IO-ARD-04 (BRANCH-COVERAGE GATE 100%)
  </read_first>
  <behavior>
    - `pytest tests/test_arduino_protocol.py -x` exits 0
    - `pytest tests/test_arduino_protocol.py --cov=pastor_tracker.io.arduino_protocol --cov-branch --cov-fail-under=100 --cov-report=term-missing` exits 0 with `Branch 100%` and empty `Missing` column
    - Every prefix (FB, FB_HEADER, READY, ERROR, SETTINGS, LIMITS, DRIVER, RESET, STOP, DIAG) has ≥ 1 well-formed test case
    - SETTINGS has ≥ 4 separate test cases (defaults / loaded / saved / structured)
    - ERROR parametrize covers all 12 ErrorCode members + 1 forward-compat unknown int
    - Malformed parametrize covers ≥ 11 distinct malformed shapes
    - Frozen + extra-forbid invariants verified
    - Limits inverted-range raises ValidationError
    - CRLF stripping (`\r`, `\n`, `\r\n`) verified
  </behavior>
  <action>
    Create pastor_tracker/tests/test_arduino_protocol.py with this structure:

    1. Module docstring: `"""Parser tests for pastor_tracker.io.arduino_protocol (IO-ARD-04 / TEST-04). 100% line + branch coverage gate. Pure parser, no fakes, no fixtures."""` followed by `from __future__ import annotations`.

    2. Imports — pytest, ValidationError from pydantic, and the public symbols from arduino_protocol.

    3. Group A — well-formed table tests, one parametrize block per prefix. Each test docstrings the requirement. Cases:
       - test_parse_ready: 3 cases (v2, v3, v255)
       - test_parse_fb: ≥ 3 cases including negative angles + accel_phase=3
       - test_parse_fb_header: 1 case
       - test_parse_settings_textual: 3 cases (defaults / loaded / saved)
       - test_parse_settings_structured: 1 case (5-tuple)
       - test_parse_limits: 1 case
       - test_parse_driver: 2 cases (ENABLED, DISABLED)
       - test_parse_reset, test_parse_stop: 1 case each
       - test_parse_diag: 2 cases (positive + negative steps)

    4. Group B — ErrorCode parametrize. Generate `(c.value, c.name) for c in ErrorCode` (12 cases). Each test asserts `parse_line(f"ERROR:{code} - {name}".encode("ascii"))` returns `Error(code=ErrorCode(code), message=name)`. Plus 1 forward-compat test: `parse_line(b"ERROR:99 - future code")` returns `Error(code=99, message="future code")` and `not isinstance(ev.code, ErrorCode)`.

    5. Group C — malformed parametrize raising ProtocolParseError. ≥ 11 cases:
       - b""
       - b"GIBBERISH"
       - b"FB:1.5,2.0"  (too few)
       - b"FB:nan,2.0,3.0,1,4,5,0"  (NaN — T-02-04)
       - b"FB:1.0,2.0,inf,1,4,5,0"  (inf)
       - b"ERROR: missing code"
       - b"ERROR:11"  (no separator)
       - b"DIAG: moving abc steps"
       - b"DRIVER:HALFON"
       - b"SETTINGS:1.0,2.0,3.0"  (wrong field count)
       - b"X" * (MAX_RX_LINE_BYTES + 1)  (over-length DoS guard)

    6. Group D — frozen + extra-forbid:
       - test_dtos_are_frozen: `Ready(version=2)` then assignment raises ValidationError
       - test_dtos_reject_extra_fields: `Ready(version=2, foo=1)` raises ValidationError
       - test_limits_rejects_inverted_range: `Limits(min_deg=90.0, max_deg=-90.0)` raises ValidationError matching "max_deg"

    7. Group E — CRLF stripping:
       - parametrize over [b"READY:v2\n", b"READY:v2\r\n", b"READY:v2\r"]; all return `Ready(version=2)`

    Tiger-style: NO mocks. NO fixtures (parser is pure). Direct `assert parse_line(b"...") == ExpectedDto(...)` everywhere.

    The executor MUST keep adding tests until `coverage report --show-missing` shows `Branch 100%` AND empty `Missing` column on arduino_protocol.py. If the parser has internal branches not covered by the table above (e.g. ASCII decode exception path), add specific tests for them.
  </action>
  <verify>
    <automated>cd pastor_tracker; .venv\Scripts\python.exe -m pytest tests/test_arduino_protocol.py -x --cov=pastor_tracker.io.arduino_protocol --cov-branch --cov-fail-under=100 --cov-report=term-missing; .venv\Scripts\ruff.exe check tests/test_arduino_protocol.py</automated>
  </verify>
  <acceptance_criteria>
    - File pastor_tracker/tests/test_arduino_protocol.py exists with ≥ 80 LOC.
    - `pastor_tracker\.venv\Scripts\python.exe -m pytest tests/test_arduino_protocol.py -x` exits 0.
    - `pastor_tracker\.venv\Scripts\python.exe -m pytest tests/test_arduino_protocol.py --cov=pastor_tracker.io.arduino_protocol --cov-branch --cov-fail-under=100 --cov-report=term-missing` exits 0.
    - Coverage report (term-missing) shows `Branch 100%` and the `Missing` column is empty for `arduino_protocol.py`. Verify with: `pastor_tracker\.venv\Scripts\python.exe -m pytest tests/test_arduino_protocol.py --cov=pastor_tracker.io.arduino_protocol --cov-branch --cov-report=term-missing | grep "arduino_protocol.py"` — last column (Missing) shows nothing after the percentage.
    - `grep -c "@pytest.mark.parametrize" pastor_tracker/tests/test_arduino_protocol.py` ≥ 5 (multiple parametrize blocks).
    - All 12 ErrorCode codes hit by the parametrize: visible in pytest verbose output: `pastor_tracker\.venv\Scripts\python.exe -m pytest tests/test_arduino_protocol.py::test_parse_error_known_codes -v 2>&1 | grep -c PASSED` ≥ 12.
    - `pastor_tracker\.venv\Scripts\ruff.exe check tests/test_arduino_protocol.py` exits 0.
    - Full Phase 2-so-far suite green: `pastor_tracker\.venv\Scripts\python.exe -m pytest -ra` exits 0.
  </acceptance_criteria>
  <done>Parser tests committed; 100% branch coverage on arduino_protocol.py; full suite green. Conventional Commit: `test(02): parser branch coverage 100% (IO-ARD-04 / TEST-04 part 1)`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Arduino USB serial → host parser | Bytes from local USB serial cross into Python; firmware is read-only and authenticated by VID:PID at transport layer (Plan 02-02), but a malformed line OR a hostile USB device matching the VID:PID still routes through this parser. |

## STRIDE Threat Register (Plan 02-01 scope: pure parser only)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering / DoS | parse_line() — long-line buffer | mitigate | Reject lines > MAX_RX_LINE_BYTES=256 with ProtocolParseError BEFORE any decode/parse work (firmware emits ≤ ~80 byte lines so 256 is generous yet bounded). |
| T-02-04b | Tampering | parse_line() — float fields | mitigate | After float() conversion, math.isfinite() check; NaN/inf raises ProtocolParseError. |
| T-02-04c | Tampering | parse_line() — non-ASCII bytes | mitigate | ASCII decode with errors="strict"; UnicodeDecodeError wrapped in ProtocolParseError. |
| T-02-04d | Information Disclosure | ProtocolParseError messages | accept | Error messages may include first 16 bytes of offending line (`text[:16]!r`) — local USB context, no PII risk. |
| T-02-04e | Tampering / Forward-compat | Error code unknown to enum | mitigate | Pitfall 9 — unknown codes kept as int (no crash) but parser flags via type union; no hidden handling. |
</threat_model>

<verification>
1. `cd pastor_tracker && .venv\Scripts\python.exe -m pytest -ra` — full Phase 2-so-far suite green (Phase 1 baseline preserved + new parser tests added).
2. `cd pastor_tracker && .venv\Scripts\python.exe -m pytest tests/test_arduino_protocol.py --cov=pastor_tracker.io.arduino_protocol --cov-branch --cov-fail-under=100 --cov-report=term-missing` — parser at 100% line + branch coverage.
3. `cd pastor_tracker && .venv\Scripts\ruff.exe check src tests` — lint clean across the whole tree.
4. `cd pastor_tracker && .venv\Scripts\mypy.exe --strict` — type-check clean across the whole package.
5. Purity smoke: `pastor_tracker\.venv\Scripts\python.exe -c "import pastor_tracker.io.arduino_protocol as m; import sys; bad = [n for n in ('serial','threading','asyncio','os','time') if any(n == k or k.startswith(n+'.') for k in sys.modules if k in dir(m))]; print('purity violations:', bad); assert not bad"` — module imports zero forbidden namespaces.
</verification>

<success_criteria>
- IO-ARD-04 parser closed: every RX prefix decoded into a frozen typed DTO; malformed lines raise ProtocolParseError with informative messages; forward-compat for unknown error codes; over-length lines rejected as DoS guard.
- 100% line + 100% branch coverage on `pastor_tracker/src/pastor_tracker/io/arduino_protocol.py`.
- pyserial 3.5 + pytest-cov 5 in pyproject.toml + uv.lock; both importable; mypy overrides for `serial.*` preserved.
- Phase 1 quality gates preserved: ruff clean, mypy --strict clean, no print(), no bare except, no magic numbers, no Any.
- Module is **pure**: zero serial/threading/asyncio/os/time imports — Plans 02-02 and 02-03 can import all DTOs and parse_line without dragging in I/O.
</success_criteria>

<output>
After completion, create `.planning/phases/02-arduino-i-o/02-01-SUMMARY.md` documenting:
- Files created/modified with LOC and purpose
- Test count and coverage (`pytest --cov=pastor_tracker.io.arduino_protocol --cov-branch --cov-report=term`)
- Public symbols exported (the <interfaces> block as final spec for 02-02 / 02-03)
- Any deviations from the plan + rationale
- Conventional Commit hashes (chore + feat + test = 3 commits)
</output>
