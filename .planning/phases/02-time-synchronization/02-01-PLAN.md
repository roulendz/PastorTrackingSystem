---
phase: 02-time-synchronization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utilities/clock.py
  - src/control/control_algorithm.py
  - src/interfaces/motor_interface.py
  - src/interfaces/camera_interface.py
  - src/control/tracker_controller.py
  - src/main.py
  - tests/conftest.py
autonomous: true

must_haves:
  truths:
    - "grep -r 'time.time()' src/ returns zero hits"
    - "grep -rn 'time.perf_counter()' src/ returns hits ONLY inside RealClock.get_time_seconds() and SimulatedMotorInterface._background_simulation_loop()"
    - "All control algorithms accept flDeltaTimeSeconds as explicit parameter"
    - "FakeClock enables deterministic testing -- same inputs produce same outputs"
    - "Existing Phase 1 tests still pass after all changes"
  artifacts:
    - path: "src/utilities/clock.py"
      provides: "Clock ABC, RealClock, FakeClock"
      exports: ["Clock", "RealClock", "FakeClock"]
    - path: "src/control/control_algorithm.py"
      provides: "Control algorithms with explicit flDeltaTimeSeconds parameter"
      contains: "def calculate_correction_from_error(self, flErrorDegrees: float, flDeltaTimeSeconds: float)"
    - path: "tests/conftest.py"
      provides: "FakeClock fixture for deterministic tests"
      contains: "obFakeClock"
  key_links:
    - from: "src/utilities/clock.py"
      to: "src/interfaces/motor_interface.py"
      via: "constructor injection obClock parameter"
      pattern: "obClock.*Clock"
    - from: "src/utilities/clock.py"
      to: "src/interfaces/camera_interface.py"
      via: "constructor injection obClock parameter"
      pattern: "obClock.*Clock"
    - from: "src/utilities/clock.py"
      to: "src/control/tracker_controller.py"
      via: "constructor injection obClock parameter"
      pattern: "obClock.*Clock"
    - from: "src/control/tracker_controller.py"
      to: "src/control/control_algorithm.py"
      via: "passes flDeltaTimeSeconds to calculate_correction_from_error()"
      pattern: "calculate_correction_from_error.*flDeltaTimeSeconds"
---

<objective>
Create the Clock abstraction (architectural keystone for Phase 2) and inject it into every component that touches time. Remove all time.time() calls and all raw time.perf_counter() calls (except inside RealClock and the background simulation sleep loop). Update control algorithm signatures to accept explicit delta time parameter.

Purpose: This is the foundation that unlocks deterministic testing and consistent timing across the entire pipeline. Every other Phase 2 plan depends on this.
Output: Clock utility module, all components using injected Clock, control algorithms accepting explicit dt, FakeClock test fixture.
</objective>

<execution_context>
@C:/Users/lives/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lives/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-time-synchronization/02-CONTEXT.md
@.planning/phases/02-time-synchronization/02-RESEARCH.md
@src/utilities/clock.py
@src/control/control_algorithm.py
@src/interfaces/motor_interface.py
@src/interfaces/camera_interface.py
@src/control/tracker_controller.py
@src/main.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Clock abstraction and update control algorithm signatures</name>
  <files>
    src/utilities/clock.py
    src/control/control_algorithm.py
    tests/conftest.py
  </files>
  <action>
    **1. Create `src/utilities/clock.py`:**
    - Clock ABC with single method `get_time_seconds() -> float` (returns monotonic seconds)
    - RealClock implementation wrapping `time.perf_counter()`
    - FakeClock implementation with:
      - `__init__(self, dStartTimeSeconds: float = 0.0)` -- stores `_dCurrentTimeSeconds`
      - `get_time_seconds() -> float` -- returns `_dCurrentTimeSeconds`
      - `advance_time_seconds(self, dDeltaSeconds: float)` -- increments current time
      - `set_time_seconds(self, dTimeSeconds: float)` -- sets absolute time
    - Use abc.ABC and @abstractmethod (per research recommendation, not Protocol)
    - Follow Hungarian notation: fl/d prefixes for float params, ob prefix for object instances

    **2. Update `src/control/control_algorithm.py`:**
    - Remove `import time` at top of file
    - Change ControlAlgorithm ABC signature:
      `calculate_correction_from_error(self, flErrorDegrees: float, flDeltaTimeSeconds: float) -> float`
    - ProportionalController: Add flDeltaTimeSeconds parameter (unused, P controller has no time dependency). Keep the simple `return self.flProportionalGain * flErrorDegrees` body.
    - PIDController:
      - Remove `self.dPreviousTime = time.time()` from __init__ (line 107) and reset_controller() (line 166)
      - Remove `dCurrentTime = time.time()` and `flDeltaTime = dCurrentTime - self.dPreviousTime` from calculate_correction_from_error() (lines 124-125)
      - Use the passed-in `flDeltaTimeSeconds` directly instead of computing it internally
      - Remove `self.dPreviousTime = dCurrentTime` from state update (line 152)
      - Keep the `if flDeltaTimeSeconds <= 0.0: flDeltaTimeSeconds = 0.001` guard (moved to use the parameter name)
    - VelocityController:
      - Remove `self.dPreviousTime = time.time()` from __init__ (line 209) and reset_controller() (line 249)
      - Remove `dCurrentTime = time.time()` and `flDeltaTime = ...` from calculate_correction_from_error() (lines 223-225)
      - Use passed-in `flDeltaTimeSeconds` directly
      - Keep the `if flDeltaTimeSeconds <= 0.0: flDeltaTimeSeconds = 0.001` guard
    - IMPORTANT: Do NOT remove self.flPreviousError from PIDController (still needed for derivative term)

    **3. Update `tests/conftest.py`:**
    - Add import: `from utilities.clock import FakeClock`
    - Add fixture:
      ```python
      @pytest.fixture
      def obFakeClock():
          """Create a FakeClock starting at time 0.0 for deterministic tests."""
          return FakeClock(dStartTimeSeconds=0.0)
      ```
    - Update obSimulatedMotor fixture to accept and use obFakeClock (but keep it working without for backward compatibility -- make clock optional via a new fixture `obSimulatedMotorWithClock` that takes obFakeClock)
  </action>
  <verify>
    - `python -c "from utilities.clock import Clock, RealClock, FakeClock; c = FakeClock(1.0); assert c.get_time_seconds() == 1.0; c.advance_time_seconds(0.5); assert c.get_time_seconds() == 1.5"` (run from src/ directory)
    - `python -c "from control.control_algorithm import PIDController; p = PIDController(); r = p.calculate_correction_from_error(1.0, 0.033); print(f'OK: correction={r:.4f}')"` (run from src/ directory)
    - `grep -rn "time.time()" src/control/control_algorithm.py` returns zero hits
  </verify>
  <done>
    Clock module exists with RealClock and FakeClock. All three control algorithms accept flDeltaTimeSeconds parameter. No time.time() calls remain in control_algorithm.py. FakeClock fixture available in conftest.py.
  </done>
</task>

<task type="auto">
  <name>Task 2: Inject Clock into all components and purge raw time calls</name>
  <files>
    src/interfaces/motor_interface.py
    src/interfaces/camera_interface.py
    src/control/tracker_controller.py
    src/main.py
  </files>
  <action>
    **1. Update `src/interfaces/motor_interface.py`:**
    - Add import: `from utilities.clock import Clock, RealClock`
    - MotorInterface.__init__: Add `obClock: Optional[Clock] = None` parameter. Store as `self._obClock = obClock or RealClock()`. Replace ALL `time.perf_counter()` calls with `self._obClock.get_time_seconds()`:
      - Line 64: `dMotorTimestampSeconds=self._obClock.get_time_seconds()` (initial state)
      - Line 151: `self._dLastCommandTimestampSeconds = self._obClock.get_time_seconds()`
      - Line 163: same for send_home_command
      - Line 329: `dNow = self._obClock.get_time_seconds()` in _parse_feedback_message
    - NullMotorInterface.__init__: Add `obClock: Optional[Clock] = None` parameter. Store `self._obClock = obClock or RealClock()`. Replace `time.perf_counter()` on lines 386 and 429 with `self._obClock.get_time_seconds()`.
    - SimulatedMotorInterface.__init__: Add `obClock: Optional[Clock] = None` parameter. Store `self._obClock = obClock or RealClock()`. Replace `time.perf_counter()` on line 462 with `self._obClock.get_time_seconds()`.
    - SimulatedMotorInterface.advance_simulation(): Replace `self._dLastUpdateTimestampSeconds = time.perf_counter()` (line 613) with `self._dLastUpdateTimestampSeconds = self._obClock.get_time_seconds()`.
    - SimulatedMotorInterface._background_simulation_loop(): KEEP the raw `time.perf_counter()` calls (lines 652-654) for the delta-time calculation of the real-time loop. This is intentional -- the background loop needs real wall-clock time for its sleep interval, not the injected clock. Add a comment explaining why.
    - IMPORTANT: Do NOT break the `import time` at the module level -- it is still needed for `time.sleep()` in the feedback loop and background simulation. But remove `time.time` usage (there should be none in motor_interface.py currently -- verify).

    **2. Update `src/interfaces/camera_interface.py`:**
    - Add import: `from utilities.clock import Clock, RealClock`
    - CameraInterface.__init__: Add `obClock: Optional[Clock] = None` parameter after sVideoFilePath. Store `self._obClock = obClock or RealClock()`.
    - capture_frame_with_timestamp(): Replace `time.perf_counter()` calls (lines 150, 156) with `self._obClock.get_time_seconds()`.
    - NOTE: The grab()/retrieve() split is in Plan 02. For now, just swap the clock source. The read() call stays for this plan.

    **3. Update `src/control/tracker_controller.py`:**
    - Add import: `from utilities.clock import Clock, RealClock`
    - TrackerController.__init__: Add `obClock: Optional[Clock] = None` parameter. Store `self._obClock = obClock or RealClock()`.
    - Replace `self.dStartTime = time.time()` (line 81) with `self.dStartTime = self._obClock.get_time_seconds()`.
    - Replace `time.time() - self.dStartTime` in get_system_statistics() (line 191) with `self._obClock.get_time_seconds() - self.dStartTime`.
    - In _execute_centering_control_algorithm(): Update the call to `self.obControlAlgorithm.calculate_correction_from_error(flAngleError)` (line 241) to pass the computed dDeltaTime: `self.obControlAlgorithm.calculate_correction_from_error(flAngleError, dDeltaTime)`.
    - The existing dt computation logic (lines 226-233) already computes dDeltaTime from sample timestamps and has a fallback. This stays. Plan 02 will refine the clamping.
    - Remove `import time` if no longer needed (check: time.time on line 81 and 191 are the only uses -- after replacement, the import may still be needed if other references exist. If not needed, remove).

    **4. Update `src/main.py`:**
    - Add import: `from utilities.clock import RealClock`
    - In initialize_system(): Create `obClock = RealClock()` at the top.
    - Pass `obClock=obClock` to:
      - SimulatedMotorInterface() constructor (lines 82, 94)
      - MotorInterface() constructor (line 87)
      - CameraInterface() constructor (line 112)
      - TrackerController() constructor (line 156)
    - Remove `import time` at the bottom of the file (line 477) if it was only used for time.sleep(2) -- replace with a top-level import if needed.

    **5. Final verification sweep:**
    - `grep -rn "time.time()" src/` must return ZERO hits
    - `grep -rn "time.perf_counter()" src/` must return hits ONLY in:
      - `src/utilities/clock.py` (RealClock.get_time_seconds)
      - `src/interfaces/motor_interface.py` (_background_simulation_loop only -- for real-time sleep delta)
    - Run `pytest tests/` to verify all Phase 1 tests still pass (they may need minor updates to pass Clock or dt parameter)

    **6. Fix any broken Phase 1 tests:**
    - tests/test_motor_simulation.py: SimulatedMotorInterface() now takes optional obClock. Existing tests pass None (default RealClock). No changes needed if default works.
    - tests/test_pipeline_no_hardware.py: If it calls calculate_correction_from_error with old signature, update to pass a dt value (e.g., 0.033 for 30fps).
    - tests/test_camera_interface.py: CameraInterface now takes optional obClock. Existing tests pass None (default). No changes needed.
    - tests/test_fov_calculation.py: Likely unaffected.
  </action>
  <verify>
    - `grep -rn "time.time()" src/` returns zero hits
    - `grep -rn "time.perf_counter()" src/` returns hits only in clock.py and motor_interface.py _background_simulation_loop
    - `pytest tests/ -v` passes all existing Phase 1 tests
    - `python -c "from utilities.clock import RealClock; c = RealClock(); t1 = c.get_time_seconds(); import time; time.sleep(0.01); t2 = c.get_time_seconds(); assert t2 > t1; print('OK')"` (run from src/)
  </verify>
  <done>
    Every component uses injected Clock. Zero time.time() calls remain in src/. Only RealClock and the background simulation loop use raw time.perf_counter(). All Phase 1 tests pass. TrackerController passes dt to control algorithms.
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "time.time()" src/` returns zero results (SYNC-01 partial)
2. `grep -rn "time.perf_counter()" src/` returns results ONLY in clock.py (RealClock) and motor_interface.py (_background_simulation_loop) (SYNC-01 complete)
3. `pytest tests/ -v` -- all existing tests pass
4. FakeClock can be used for deterministic testing: create FakeClock, advance manually, verify timestamps match
5. Control algorithms accept explicit dt parameter (SYNC-05 partial -- full verification in Plan 02 with clamping)
</verification>

<success_criteria>
- Clock module created with RealClock + FakeClock supporting DI (per locked decision)
- ALL time.time() removed from src/ (SYNC-01)
- ALL raw time.perf_counter() removed except RealClock and background simulation loop (SYNC-01)
- Control algorithms accept flDeltaTimeSeconds parameter (per locked decision: SYNC-05)
- Motor thread uses shared Clock utility (per locked decision)
- TrackingSample continues to carry absolute timestamp (per locked decision -- unchanged)
- All Phase 1 tests pass unchanged or with minimal dt parameter additions
</success_criteria>

<output>
After completion, create `.planning/phases/02-time-synchronization/02-01-SUMMARY.md`
</output>
