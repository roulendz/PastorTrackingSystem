---
phase: 01-test-foundation-and-code-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/interfaces/motor_interface.py
  - src/control/tracker_controller.py
  - src/utilities/config_manager.py
  - src/main.py
  - src/ui/live_settings_panel.py
  - src/ui/config_editor.py
  - src/tracking/pose_tracker.py
  - src/interfaces/camera_interface.py
autonomous: true

must_haves:
  truths:
    - "No bare 'except Exception: pass' blocks exist in the codebase -- every except block either catches a specific exception type or logs with traceback"
    - "Unused camera exposure methods (set_camera_exposure, enable_auto_exposure) are removed"
    - "Unused _obPreviousMotorState field is removed from MotorInterface"
    - "dataclasses-json is not imported or referenced anywhere in src/"
    - "Every module and function is reachable from main.py or tests"
  artifacts:
    - path: "src/interfaces/motor_interface.py"
      provides: "Specific exception types in feedback parsing, _obPreviousMotorState removed"
      contains: "except ValueError"
    - path: "src/control/tracker_controller.py"
      provides: "Specific exception types with logging in config getattr calls"
      contains: "except AttributeError"
    - path: "src/main.py"
      provides: "Silent exception blocks fixed with logging"
      contains: "logger.debug"
    - path: "src/ui/live_settings_panel.py"
      provides: "Silent UI exception blocks have logger.debug calls"
      contains: "logger.debug"
  key_links:
    - from: "src/interfaces/motor_interface.py"
      to: "Arduino serial feedback"
      via: "Specific ValueError/TypeError catch in timestamp parsing"
      pattern: "except (ValueError, TypeError)"
    - from: "src/control/tracker_controller.py"
      to: "src/utilities/config_manager.py"
      via: "AttributeError catch with default fallback on config getattr"
      pattern: "except AttributeError"
---

<objective>
Clean the codebase by fixing all silent exception swallowing (CODE-02) and removing dead code (CODE-01). Every except block gets specific exception types and logging. Every unused function, field, and dependency is removed.

Purpose: A clean codebase makes all later phases easier to debug and maintain. Silent exception swallowing has been hiding bugs (including potentially the timing drift).
Output: All source files updated with proper exception handling and dead code removed.
</objective>

<execution_context>
@C:/Users/lives/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lives/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-foundation-and-code-cleanup/01-RESEARCH.md

@src/interfaces/motor_interface.py
@src/control/tracker_controller.py
@src/utilities/config_manager.py
@src/main.py
@src/ui/live_settings_panel.py
@src/ui/config_editor.py
@src/tracking/pose_tracker.py
@src/interfaces/camera_interface.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all silent exception swallowing across codebase</name>
  <files>src/interfaces/motor_interface.py, src/control/tracker_controller.py, src/utilities/config_manager.py, src/main.py, src/ui/live_settings_panel.py, src/ui/config_editor.py, src/tracking/pose_tracker.py</files>
  <action>
Fix every `except Exception: pass` and `except Exception` block in the codebase. Use the research audit as a guide. The three categories are:

**CRITICAL PATH (MUST catch specific exceptions and log):**

1. `motor_interface.py` line ~345 (timestamp parsing `except Exception`):
   - Change to: `except (ValueError, TypeError) as e:` with `logger.warning(f"Failed to parse Arduino timestamp: {e}")`
   - This block parses Arduino `micros()` timestamps -- ValueError for bad int conversion, TypeError for None values.

2. `tracker_controller.py` line ~109 (`get_estimated_motor_angle_degrees` catch):
   - Change to: `except (ValueError, TypeError, AttributeError) as e:` with `logger.debug(f"Motor angle estimation fallback: {e}")`
   - This is a fallback to latest state when interpolation fails -- debug level since fallback is fine.

3. `tracker_controller.py` lines ~264 and ~269 (config getattr catches):
   - Change to: `except (AttributeError, TypeError, ValueError) as e:` with `logger.debug(f"Config fallback for home return velocity: {e}")`
   - These read optional config values with getattr fallback -- debug level.

4. `config_manager.py` lines ~125 and ~224 (FOV extraction from nested JSON):
   - Change to: `except (KeyError, TypeError, AttributeError) as e:` with `logger.debug(f"No UI/controls FOV override in config: {e}")`
   - These try to extract a nested FOV value from UI config -- missing is expected.

5. `config_editor.py` line ~210 (config save):
   - Change to: `except (IOError, OSError, json.JSONDecodeError) as e:` with `logger.error(f"Failed to save configuration: {e}")`
   - Config save failure should be visible to the user.

6. `main.py` line ~424 (FOV calibration entire block):
   - Change to: `except (ValueError, TypeError, AttributeError, ZeroDivisionError) as e:` with `logger.error(f"FOV calibration error: {e}")` and `dFovCalibration = None` (reset state on error).

**UI PATH (keep but add logging):**

7. `main.py` lines ~203, ~415, ~420, ~435 (UI slider update catches):
   - Change each to: `except Exception as e:` with `logger.debug(f"UI slider update skipped: {e}")`
   - Keep broad catch because these are UI convenience calls that may fail for many reasons (panel not open, widget not created yet, import failure).

8. `live_settings_panel.py` -- all instances (lines ~108, 192, 201, 257, 271, 298, 351, 434, 441, 554, 559, 601):
   - Change each `except Exception: pass` to `except Exception: logger.debug("DearPyGui operation skipped")`
   - These are DearPyGui widget operations that fail when UI is not fully initialized. Broad catch is acceptable for UI.

9. `live_settings_panel.py` line ~320 (config save):
   - Change to: `except (IOError, OSError) as e:` with `logger.error(f"Failed to save config from settings panel: {e}")`

10. `live_settings_panel.py` lines ~375, ~417 (screen metrics / font scale):
    - Change to: `except Exception as e:` with `logger.debug(f"Screen metrics fallback: {e}")`
    - Keep broad catch -- platform-specific failures.

**CLEANUP PATH (destructors -- keep broad catch):**

11. `pose_tracker.py` lines ~182, ~186, ~269 (MediaPipe detector close):
    - Change each to: `except Exception: logger.debug("MediaPipe cleanup in destructor")`
    - Destructors must not raise. Broad catch is correct here.

**Important guidelines:**
- Never change `except Exception as e: logger.error(...)` blocks that already log -- these are already handled correctly (like motor_interface.py line ~379, _send_command line ~268).
- Add `import json` at top of config_editor.py if not already there (needed for json.JSONDecodeError).
- Ensure every `except` block has a body (no bare `pass` without logging).
- Use `logger.debug()` for expected/acceptable failures, `logger.warning()` for unexpected-but-recoverable, `logger.error()` for failures requiring attention.
  </action>
  <verify>
Search for remaining bare `except Exception: pass` or `except Exception:\n            pass` patterns:
```bash
cd src && grep -rn "except.*:$" --include="*.py" | head -50
grep -rn "except Exception:" --include="*.py" . | grep -v "logger" | grep -v "# " | head -20
```
Every `except` block should either:
- Catch specific exception types (ValueError, TypeError, etc.), OR
- Have a `logger.` call in the body

No `except Exception: pass` (bare pass without logging) should remain anywhere.
  </verify>
  <done>Every except block in the codebase either catches specific exception types or has logging. Zero instances of bare `except Exception: pass` remain. Critical path exceptions catch specific types (ValueError, TypeError, etc.). UI exceptions have debug logging. Destructor exceptions have debug logging.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead code and unused dependencies</name>
  <files>src/interfaces/motor_interface.py, src/interfaces/camera_interface.py</files>
  <action>
Remove confirmed dead code identified in the research audit. For each removal, verify no callers exist first by grepping the entire codebase.

**Removals:**

1. `camera_interface.py` -- Remove `set_camera_exposure()` method (lines ~139-150):
   - Verify: grep entire src/ for `set_camera_exposure` -- should find only the definition.
   - Remove the method entirely.

2. `camera_interface.py` -- Remove `enable_auto_exposure()` method (lines ~152-156):
   - Verify: grep entire src/ for `enable_auto_exposure` -- should find only the definition.
   - Remove the method entirely.

3. `motor_interface.py` -- Remove `_obPreviousMotorState` field:
   - In `MotorInterface.__init__()` (line ~66): remove `self._obPreviousMotorState: Optional[MotorState] = None`
   - In `_parse_feedback_message()` (lines ~350-357): remove the block that copies current state to `_obPreviousMotorState` before updating. The state history deque already serves this purpose.
   - Verify: grep for `_obPreviousMotorState` -- should only find the definition and this one usage.

4. DO NOT remove these (verified as in-use or potentially useful):
   - `validate_configuration()` in config_manager.py -- useful for future validation
   - `get_frame_dimensions()` in camera_interface.py -- called from live_settings_panel.py and tracker_controller.py
   - `TextRenderer` class -- provides consistent text rendering
   - `_flLastCommandedTargetAngleDegrees` and `_dLastCommandTimestampSeconds` in motor_interface.py -- keep for debugging value (low cost)
   - `dataclasses-json` from requirements.txt -- already removed in Plan 01 Task 3

**Verification approach for each removal:**
Before removing any code, grep the entire project for the function/field name as both a definition and as a string literal (to catch dynamic access via getattr or config references).
  </action>
  <verify>
1. Verify removed methods do not exist:
```bash
grep -rn "set_camera_exposure\|enable_auto_exposure" src/ --include="*.py"
grep -rn "_obPreviousMotorState" src/ --include="*.py"
```
All should return empty.

2. Verify the application still imports cleanly:
```python
import sys; sys.path.insert(0, 'src')
from interfaces.motor_interface import MotorInterface, NullMotorInterface, MotorState
from interfaces.camera_interface import CameraInterface
print("All imports OK")
```

3. Verify kept methods still exist:
```python
from interfaces.camera_interface import CameraInterface
assert hasattr(CameraInterface, 'get_frame_dimensions')
assert hasattr(CameraInterface, 'capture_frame_with_timestamp')
from utilities.config_manager import ConfigurationManager
assert hasattr(ConfigurationManager, 'validate_configuration')
print("Kept methods OK")
```
  </verify>
  <done>Dead code removed: set_camera_exposure(), enable_auto_exposure(), _obPreviousMotorState field and its copy logic. All remaining code is reachable from main.py or tests. No functionality lost.</done>
</task>

</tasks>

<verification>
1. `grep -rn "except.*pass" src/ --include="*.py"` returns zero results (no bare except-pass patterns)
2. `grep -rn "set_camera_exposure\|enable_auto_exposure\|_obPreviousMotorState" src/` returns zero results
3. All modules import without error
4. `grep -rn "dataclasses.json\|dataclasses_json" src/` returns zero results (no references to removed dependency)
</verification>

<success_criteria>
- Zero instances of `except Exception: pass` (bare pass without logging) in the entire codebase
- Every except block either catches specific types or has a logger call
- set_camera_exposure() and enable_auto_exposure() removed from camera_interface.py
- _obPreviousMotorState removed from motor_interface.py
- All remaining code is reachable and imports work
</success_criteria>

<output>
After completion, create `.planning/phases/01-test-foundation-and-code-cleanup/01-02-SUMMARY.md`
</output>
